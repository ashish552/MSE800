import  numpy as np 
integers = np.arange(1,11)
print(f"the first ten positive intergers are: {integers}")
array_shape = integers.shape
print(f"The shape of array is {array_shape}")
data_type = integers.dtype
print(f"The data type is: {data_type}")
product = integers * 2
print (product)

